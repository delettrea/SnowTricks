{% extends 'base.html.twig' %}

{% block body %}
    <h1>Illustration creation</h1>

    {{ form_start(form) }}
    <input type="submit" value="Edit" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('trick_details', { 'id': tricks.id }) }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
