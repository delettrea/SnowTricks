{% extends 'base.html.twig' %}

{% block body %}
    <h1>Video edit</h1>

    {{ form_start(form) }}
    <input type="submit" value="Edit" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('trick_details', { 'id': tricks.id }) }}">Retour à la figure.</a>
        </li>
    </ul>
{% endblock %}
